demonstration server for angular 2 italk.

devlivers simple rest interface to get temperature and humidity values.

run 
```bash
npm install
npm install -g gulp
```

build with:
```bash
gulp scripts
```

run with:
```bash
npm start
```